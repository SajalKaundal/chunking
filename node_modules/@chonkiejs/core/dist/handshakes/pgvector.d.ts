import { Chunk } from "../types.js";
import { BaseHandshake, BaseHandshakeOptions, HandshakeSearchResult } from "./base.js";

//#region src/handshakes/pgvector.d.ts
interface PgvectorHandshakeOptions extends BaseHandshakeOptions {
  /** A pg Pool or Client instance with pgvector extension enabled. */
  client: PgClient;
  /** Table name. (default: 'chunks') */
  tableName?: string;
  /** Whether to auto-create the table on first write. (default: true) */
  autoCreateTable?: boolean;
}
interface PgClient {
  query(text: string, values?: unknown[]): Promise<{
    rows: Array<Record<string, unknown>>;
    rowCount: number;
  }>;
}
declare class PgvectorHandshake extends BaseHandshake {
  private client;
  private tableName;
  private autoCreateTable;
  private tableCreated;
  constructor(options: PgvectorHandshakeOptions);
  private ensureTable;
  write(chunks: Chunk | Chunk[]): Promise<void>;
  search(query: string, options?: {
    limit?: number;
  }): Promise<HandshakeSearchResult[]>;
  searchByVector(embedding: number[], options?: {
    limit?: number;
  }): Promise<HandshakeSearchResult[]>;
  count(): Promise<number>;
}
//#endregion
export { PgClient, PgvectorHandshake, PgvectorHandshakeOptions };
//# sourceMappingURL=pgvector.d.ts.map