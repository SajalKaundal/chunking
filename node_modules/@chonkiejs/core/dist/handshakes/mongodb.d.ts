import { Chunk } from "../types.js";
import { BaseHandshake, BaseHandshakeOptions, HandshakeSearchResult } from "./base.js";

//#region src/handshakes/mongodb.d.ts
interface MongoDBHandshakeOptions extends BaseHandshakeOptions {
  /** Pre-initialized MongoDB collection. */
  collection: MongoCollection;
  /** Name of the Atlas vector search index. (default: 'vector_index') */
  indexName?: string;
}
interface MongoCollection {
  insertMany(docs: Array<Record<string, unknown>>): Promise<{
    insertedCount: number;
  }>;
  bulkWrite(ops: Array<Record<string, unknown>>): Promise<unknown>;
  aggregate(pipeline: Array<Record<string, unknown>>): {
    toArray(): Promise<Array<Record<string, unknown>>>;
  };
  countDocuments(): Promise<number>;
}
declare class MongoDBHandshake extends BaseHandshake {
  private collection;
  private indexName;
  constructor(options: MongoDBHandshakeOptions);
  write(chunks: Chunk | Chunk[]): Promise<void>;
  search(query: string, options?: {
    limit?: number;
  }): Promise<HandshakeSearchResult[]>;
  searchByVector(embedding: number[], options?: {
    limit?: number;
  }): Promise<HandshakeSearchResult[]>;
  count(): Promise<number>;
}
//#endregion
export { MongoCollection, MongoDBHandshake, MongoDBHandshakeOptions };
//# sourceMappingURL=mongodb.d.ts.map