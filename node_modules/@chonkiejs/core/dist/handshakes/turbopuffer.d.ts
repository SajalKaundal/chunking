import { Chunk } from "../types.js";
import { BaseHandshake, BaseHandshakeOptions, HandshakeSearchResult } from "./base.js";

//#region src/handshakes/turbopuffer.d.ts
interface TurbopufferHandshakeOptions extends BaseHandshakeOptions {
  /** Pre-initialized Turbopuffer namespace. */
  namespace: TurbopufferNamespace;
}
interface TurbopufferNamespace {
  upsert(params: {
    ids: string[];
    vectors: number[][];
    attributes?: Record<string, Array<string | number | boolean | null>>;
  }): Promise<void>;
  query(params: {
    vector: number[];
    top_k: number;
    include_attributes?: boolean;
  }): Promise<Array<{
    id: string;
    dist: number;
    attributes?: Record<string, string | number | boolean | null>;
  }>>;
  exists(): Promise<{
    approx_count?: number;
  }>;
}
declare class TurbopufferHandshake extends BaseHandshake {
  private namespace;
  constructor(options: TurbopufferHandshakeOptions);
  write(chunks: Chunk | Chunk[]): Promise<void>;
  search(query: string, options?: {
    limit?: number;
  }): Promise<HandshakeSearchResult[]>;
  searchByVector(embedding: number[], options?: {
    limit?: number;
  }): Promise<HandshakeSearchResult[]>;
  count(): Promise<number>;
}
//#endregion
export { TurbopufferHandshake, TurbopufferHandshakeOptions, TurbopufferNamespace };
//# sourceMappingURL=turbopuffer.d.ts.map