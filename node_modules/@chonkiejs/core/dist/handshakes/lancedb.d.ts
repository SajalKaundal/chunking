import { Chunk } from "../types.js";
import { BaseHandshake, BaseHandshakeOptions, HandshakeSearchResult } from "./base.js";

//#region src/handshakes/lancedb.d.ts
interface LanceDBHandshakeOptions extends BaseHandshakeOptions {
  /** Pre-initialized LanceDB table. */
  table: LanceTable;
}
interface LanceTable {
  add(data: Array<Record<string, unknown>>): Promise<void>;
  search(vector: number[]): LanceQuery;
  countRows(): Promise<number>;
}
interface LanceQuery {
  limit(n: number): LanceQuery;
  toArray(): Promise<Array<Record<string, unknown>>>;
}
declare class LanceDBHandshake extends BaseHandshake {
  private table;
  constructor(options: LanceDBHandshakeOptions);
  write(chunks: Chunk | Chunk[]): Promise<void>;
  search(query: string, options?: {
    limit?: number;
  }): Promise<HandshakeSearchResult[]>;
  searchByVector(embedding: number[], options?: {
    limit?: number;
  }): Promise<HandshakeSearchResult[]>;
  count(): Promise<number>;
}
//#endregion
export { LanceDBHandshake, LanceDBHandshakeOptions, LanceQuery, LanceTable };
//# sourceMappingURL=lancedb.d.ts.map